# SPEC.md - 体験仕様

## 画面構成

1画面（3ペイン構成）

```
┌────────────┬──────────────────────────────────────┐
│            │  タイトル入力欄                        │
│  サイドバー ├──────────────────┬───────────────────┤
│  （ノート  │   エディタ        │   プレビュー       │
│   一覧）   │   （左ペイン）    │   （右ペイン）     │
│            │                  │                   │
└────────────┴──────────────────┴───────────────────┘
```

---

## サイドバー

- ノートのタイトル一覧を表示（新しい順）
- 「+ 新規ノート」ボタン
- 選択中のノートはハイライト表示
- 各ノートの右端に削除ボタン（🗑 アイコン）

---

## エディタ部分（上部：タイトル欄 / 下部：左右ペイン）

### タイトル欄
- テキスト入力フィールド（1行）
- プレースホルダー: 「タイトルを入力...」
- 入力のたびに自動保存（localStorageに反映）

### エディタ（左ペイン）
- `<textarea>` でマークダウンを入力
- プレースホルダー: 「# 見出し」「**太字**」「- 箇条書き」などのヒントを表示
- 入力のたびに自動保存 & リアルタイムプレビュー更新

### プレビュー（右ペイン）
- マークダウンをHTMLに変換して表示（`marked` 使用）
- エディタの内容と常に同期
- 見出し・箇条書き・太字・コードブロックなどが整形表示される

---

## レスポンシブ対応

| 画面幅 | 表示 |
|--------|------|
| 広い画面（PC） | サイドバー + エディタ + プレビューの3ペイン表示 |
| 狭い画面（スマホ） | 上部に「編集」「プレビュー」タブを表示し切り替え |

---

## ノートの操作

### 新規作成
- 「+ 新規ノート」をクリック
- タイトル「無題」・本文空欄のノートが作成される
- 作成したノートが自動的に選択状態になる

### 切り替え
- サイドバーのタイトルをクリックで切り替え
- 切り替えると前のノートは自動保存済み

### 削除
- 削除ボタンをクリック → 確認ダイアログ表示
- 「本当に削除しますか？」→ OK で削除・キャンセルで中断
- 削除後は隣のノートを自動選択（なければ空状態）

---

## 初期状態

- 初回アクセス時: サンプルノートを1つ表示（マークダウンの使い方を説明する内容）
- 2回目以降: 前回のノートをそのまま復元

---

## データ保存（localStorage）

- 入力するたびに自動保存（保存ボタンなし）
- ページをリロードしてもノートが残る
